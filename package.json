{
  "name": "quickfix-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd quickfix_backend && npm install --production=false",
    "postinstall": "cd quickfix_backend && npm install --production=false",
    "start": "cd quickfix_backend && npm start"
  }
}
